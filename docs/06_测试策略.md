# 鱿郁仔仔 测试策略

> 版本：v1.0 | 更新日期：2026-05-23

---

## 一、测试分层

```
┌─────────────────────────────────┐
│        E2E 端到端测试            │  ← 真实Switch设备，低频
│    (playwright + 真实设备)       │
├─────────────────────────────────┤
│        Integration 集成测试       │  ← Mock外部依赖，中频
│    (pytest + mock)               │
├─────────────────────────────────┤
│        Unit 单元测试              │  ← 每个模块独立，高频
│    (pytest)                      │
└─────────────────────────────────┘
```

---

## 二、Python 核心服务测试

### 2.1 爬虫模块（scraper/）

| 测试用例 | 类型 | Mock策略 |
|---|---|---|
| 搜索"塞尔达"返回非空列表 | Unit | Mock Playwright页面HTML |
| 搜索不存在的游戏返回空列表 | Unit | Mock 空结果HTML |
| 详情页解析获取网盘链接 | Unit | Mock 详情页HTML |
| Cookie过期后自动重新登录 | Integration | 真实Cookie测试 |
| 网站不可达时返回明确错误（非崩溃） | Unit | Mock 网络超时 |
| 模糊匹配："野炊" → "塞尔达传说 旷野之息" | Unit | 词典匹配 |

### 2.2 网盘模块（cloud_disk/）

| 测试用例 | 类型 | Mock策略 |
|---|---|---|
| 夸克Cookie有效性检测 | Unit | Mock HTTP响应 |
| 文件转存API调用 | Unit | Mock API响应 |
| 下载进度回调触发正确 | Unit | Mock 分块下载 |
| 下载中断后断点续传 | Integration | 本地文件Mock |
| 百度/阿里云盘文件转存（VIP） | Unit | Mock API响应 |

### 2.3 MTP模块（mtp/）

| 测试用例 | 类型 | Mock策略 |
|---|---|---|
| 无设备连接返回空列表 | Unit | Mock PyMTP |
| 检测到Switch设备返回正确VID/PID | Unit | Mock设备列表 |
| 文件传输进度回调（每1MB触发） | Unit | Mock文件传输 |
| TF卡空间不足时切换到NAND | Unit | Mock空间查询 |
| **真实Switch文件传输** | E2E | 需真实设备 |

### 2.4 游戏文件识别（game_files/）

| 测试用例 | 类型 | Mock策略 |
|---|---|---|
| 规范目录：本体/更新/DLC分文件夹 | Unit | 本地测试fixture |
| 单文件目录：仅.nsp文件 | Unit | 本地测试fixture |
| 金手指.zip文件识别 | Unit | 本地测试fixture |
| 乱命名文件正确分类 | Unit | 后缀匹配 |
| 空目录返回空结果 | Unit | 空目录fixture |
| 安装顺序：本体 → 更新 → DLC → 金手指 | Unit | 排序验证 |

### 2.5 缓存模块（cache/）

| 测试用例 | 类型 | Mock策略 |
|---|---|---|
| 缓存写入 + 读取验证 | Unit | 临时目录 |
| 1天TTL过期后自动失效 | Unit | Mock文件时间 |
| 缓存命中率统计 | Integration | 多次安装 |

### 2.6 FastAPI API测试

| 测试用例 | 类型 |
|---|---|
| `/api/health` 返回 ok | Unit (TestClient) |
| `/api/search` 返回正确JSON结构 | Unit (TestClient) |
| `/api/install` 返回任务ID | Unit (TestClient) |
| `/api/install/{id}/progress` 返回进度 | Unit (TestClient) |
| 无效参数返回400 | Unit (TestClient) |
| CORS头部正确 | Unit (TestClient) |

---

## 三、Electron GUI 测试

### 3.1 组件单元测试（Vitest + React Testing Library）

| 测试用例 |
|---|
| SearchBox 输入防抖正常工作 |
| GameList 空状态显示正确 |
| InstallButton 各状态切换（默认/安装中/完成/失败） |
| ProgressBar 进度渲染 |
| Settings 配置读写 |
| 设备状态指示器颜色变化 |

### 3.2 集成测试（Playwright + Electron）

| 测试用例 |
|---|
| 启动 → 后端自动拉起 → health检测通过 → 显示主界面 |
| 搜索输入 → 结果列表渲染 |
| 点击安装 → 进度更新 → 完成提示 |
| 设置页保存后重启设置值保持 |
| 异常场景：后端挂掉时显示错误 + 重试按钮 |

---

## 四、Cloudflare Workers 测试

### 4.1 授权API

| 测试用例 | 工具 |
|---|---|
| 有效兑换码激活返回 license_key | wrangler dev + curl |
| 已使用的兑换码再次激活返回错误 | wrangler dev + curl |
| 过期license验证返回 false | wrangler dev + curl |
| 心跳上报正常 | wrangler dev + curl |
| 超30天未心跳自动吊销 | 集成测试（Mock时间） |

### 4.2 管理后台

| 测试用例 | 工具 |
|---|---|
| 管理员登录 → 生成兑换码 → 码可正常激活 | Playwright + wrangler |
| 禁用用户后license验证返回false | Playwright + wrangler |

---

## 五、E2E 端到端测试

### 5.1 完整安装流程（需真实Switch）

| 步骤 | 验证点 |
|---|---|
| 启动软件 | Python后端启动、Electron窗口正常显示 |
| 设备检测 | Switch已连接状态正常 |
| 搜索游戏 | 返回正确结果列表 |
| 选择并安装 | 网盘下载 → MTP传输 → DBI安装 全流程 |
| 安装完成通知 | PushDeer推送到达手机 |

### 5.2 大气层升级流程（需真实Switch + TF卡）

| 步骤 | 验证点 |
|---|---|
| 插入TF卡 | 检测到TF卡 |
| 执行升级 | 旧文件清理、新固件下载、解压部署 |
| 验证 | TF卡文件结构正确，nintendo/emummc未被删除 |

---

## 六、CI 自动化测试矩阵

| 触发 | 执行内容 |
|---|---|
| PR 到 main | Python unit tests + React unit tests + lint (ruff + eslint) |
| Push 到 main | 以上所有 + 构建验证 + Cloudflare wrangler deploy (staging) |
| Release tag | 以上所有 + PyInstaller打包 + electron-builder打包 + 发布到GitHub Release |