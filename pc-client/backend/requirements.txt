# 鱿郁仔仔 - Python 核心服务依赖
# 安装: pip install -r requirements.txt

# Web 框架
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# HTTP 客户端
httpx>=0.27.0

# 浏览器自动化（爬虫）
playwright>=1.42.0

# MTP 文件传输（Switch DBI）
# PyMTP 需要系统安装 libmtp
# Windows: 下载 libmtp-9.dll + libusb-1.0.dll
# https://pypi.org/project/PyMTP/

# SMB 协议（NAS 通信）
pysmb>=1.2.9

# 数据校验
pydantic>=2.6.0
python-multipart>=0.0.9

# 工具
python-dotenv>=1.0.0
rich>=13.7.0          # 美观的终端输出（CLI 模式调试用）

# 测试
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0
httpx  # FastAPI TestClient 依赖
